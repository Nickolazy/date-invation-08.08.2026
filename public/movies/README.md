Drop movie posters here, named to match `poster` paths in `src/data/invitation.ts`
(e.g. `movie-1.webp`). Until a file exists at that path, `MovieCard` falls back
to a small decorative placeholder automatically — nothing else needs to change.
