Drop the background track here as:

- `our-song.mp3`

It loops quietly in the background across every screen. Until a file
exists at that path, the browser just fails to load it silently — the
music toggle button still renders, it simply has nothing to play.
